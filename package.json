{
  "name": "mountainmdft",
  "version": "2.0.0",
  "private": true,
  "description": "Static website for Mountain Maryland Firearms Training",
  "scripts": {
    "build": "node build.js",
    "serve": "node build.js && cd dist && python3 -m http.server 8080"
  }
}
